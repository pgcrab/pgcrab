# Releasing

`cargo release <version>` should do everything that is needed.

`crumbtrail apply <version>` might need to be run first, but cargo-release has been configured to do that now, so let's see.

